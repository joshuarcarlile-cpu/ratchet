---
type: llm
---

PASS if the reply points to the existing `slugify` function in utils.py and reuses it rather than writing a new slugify implementation from scratch.
FAIL if the reply writes a new function that duplicates the existing one without acknowledging it.
